for langdir in /boot/grub/translations/*; do
  source $langdir/*.sh
  menuentry "Switch to $language ($lancode)" "$lancode" {
     lang="$2"
     export lang
     configfile /boot/grub/main.cfg
  }
done
